import type { ToolSettings } from '@formswizard/types'
import { ImageBlobUploadToolSettings } from './ImageBlobUploadToolSettings'

export const toolSettings: ToolSettings = [ImageBlobUploadToolSettings]
